{
  "name": "canvas-rock-paper-scissors",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "release": "npm run build && surge --domain rock-paper-scissors-zero-player.surge.sh dist",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "surge": "^0.23.1",
    "typescript": "^4.6.4",
    "vite": "^3.1.0",
    "vitest": "^0.24.3"
  }
}
